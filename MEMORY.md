# MEMORY - bàn giao sang đoạn chat mới

> Cập nhật: 2026-08-10 · Đối chiếu **Đợt 9 - chín mục chủ dự án nêu sau khi dùng thử**.
> File này **cố ý không chép lại `CODEMAP.md`**. CODEMAP trả lời *"code nằm đâu, sửa thì gãy gì"*.
> File này trả lời: ***đang ở đâu, làm gì tiếp, và cách làm việc trong repo này.***

---

## 0. Đọc gì, theo thứ tự

1. [CLAUDE.md](CLAUDE.md) - luật ngắn.
2. [CODEMAP.md](CODEMAP.md) - **§8** (sửa X thì đụng đâu) · **§9** (bất biến) · **§10** (bẫy đã gặp). Ba mục này phải xem **trước khi gõ dòng đầu tiên**.
3. File này.

> `KE-HOACH-DOT-TIEP-THEO.md` là **tài liệu lịch sử** - kế hoạch cho QR / yếm / sổ thu hoạch / chợ, đã làm xong hết. Đừng đọc nó như việc còn phải làm.

---

## 1. Sản phẩm, trong một đoạn

**ChicChic** - nhận nuôi một chuồng gà thật ở Ba Vì, chăm qua app: giao việc cho nông dân, nhận ảnh mỗi ngày, cuối chu kỳ chọn nhận thịt / cho nghỉ hưu / nuôi lứa mới. **Không phải kênh đầu tư, không cam kết lợi nhuận.** Ba trụ giữ sản phẩm đứng: *việc chỉ xong khi có ảnh minh chứng* · *nông dân là người thật xem được mặt* · *tin xấu cũng báo thật*.

Next.js 14 App Router · Prisma 5.22 · Supabase Postgres `ap-southeast-1` (pooler 6543) · Vercel ghim vùng `sin1`. Ngôn ngữ của **mọi thứ** trong repo - giao diện, comment, commit - là **tiếng Việt**.

---

## 2. Các đợt gần nhất

| Commit | Việc |
|---|---|
| *(đợt này)* | **Đường hoàn tiền (Đợt 10) - và một lỗ rò lòi ra ngay đầu đợt.** ⚠️ **Lỗ rò trước đã:** `returnBarn` đặt `ownerId = null`, mà `lib/auth` lại coi `!ownerId` là lý do cho xem - từ Đợt 9 `barnViewer` phục vụ **khách chưa đăng nhập**, nên ai hoàn trả chuồng thì tên chuồng + cả cuốn nhật ký ảnh **mở toang ra internet**. Đo thật bằng `curl` không cookie trên bản production: HTTP 200 kèm đủ nội dung, **3 chuồng thật** đang như vậy (§11.37). Đã bỏ `!ownerId` khỏi cả hai cổng, `tests/hoan-tien.test.ts` khoá lại bằng bề mặt mã nguồn. **Việc chính:** hoàn trả chuồng nay **ghi nợ phần tiền nuôi của những ngày chưa nuôi** (theo tỉ lệ ngày, trong **cùng transaction** với phép gỡ quyền sở hữu) · người mua báo hàng chợ không đúng trong **3 ngày** ⟹ ghi nợ **trọn giá kể cả phí** · bàn `↩️ Hoàn tiền` ở `/admin` (duyệt · từ chối có lý do · đóng sổ kèm ảnh biên lai và **số THẬT đã chuyển**). Câu cũ ở ô hoàn trả hứa *"cọc đối soát hoàn lại"* và câu trả về hứa *"hoàn lại theo chính sách"* - **cả hai đều rỗng**, nay nói thẳng con số. **DB thật đã đổi**: `CREATE TYPE RefundKind/RefundStatus` + `CREATE TABLE "Refund"` (thuần additive). 

**Phần hai của đợt - lứa mới hết miễn phí (§11.17).** Bấm *"nuôi lứa mới"* trước đó reset đàn gà nhưng **không đụng gì tới tiền**: mốc tính tiền luôn là ngày cọc, mà `soHoaDonCanCo` với **gà thịt** trả đúng 1 mãi mãi ⟹ lứa thứ hai trở đi nuôi trọn 75 ngày **không tốn đồng nào**, bấm ba lần là ba lứa miễn phí. Nay `Barn.billingFrom` + `billingSeqBase` đặt lại mốc ngay trong nhánh đó. Cùng cột ấy chữa một lỗi **ngược chiều**: chuồng **gà đẻ** nằm `END_OF_LAY` hai tháng rồi mới bấm thì bị **truy thu** cả hai tháng không ai nuôi. Và màn kết chu kỳ nay **nói giá trước khi bấm** - im lặng ở chỗ có tiền thì người đọc mặc định là miễn phí. `kyConThieu` được tách ra khỏi `ensureInvoices` để phần đụng tiền nằm được trong bộ kiểm một giây. **DB**: `Barn` thêm `billingFrom` + `billingSeqBase`. 284 → **310** phép kiểm |
| `5b025da` | **Chín mục chủ dự án nêu sau khi dùng thử.** Ba commit: `a592435` (khách xem thử + bốn dòng cam kết bấm được + ẩn mời mọc + ô tài khoản ngân hàng) · `c49ef17` (sổ cân hằng tuần, đổi tên gà, nhờ cấp đông) · `5b025da` (ví & rút tiền). Chi tiết ở §2c bên dưới. **DB thật đã đổi**: `ALTER TYPE "TaskKind"` thêm `FREEZE` + `WEIGH` · `CREATE TABLE "WeighIn"` · `Payout` thêm `requestedAt`. Bất biến **§9.5 được NỚI** (khách xem được chuồng trưng bày) - đọc lại §9.5 trước khi đụng vào cổng quyền chuồng |
| *(đợt này, phần ba)* | **Bộ kiểm phủ CỔNG QUYỀN (Đợt 11, §11.18).** Lý do làm ngay: §11.37 nằm **ngay trong `canViewBarn`/`barnViewer`** - cửa quyền trung tâm - và cả bốn lệnh kiểm đều xanh suốt thời gian nó tồn tại. Lần **thứ hai** một thứ hỏng toàn phần đi qua bốn đèn xanh (lần đầu: kho ảnh thiếu `apikey`). Cách chữa **không** phải dựng DB thứ hai cho test (cần Docker / một Supabase nữa) mà là kéo phần rủi ro ra thành **hàm thuần** rồi phủ kín - cùng mẹo đã dùng cho `kyConThieu`, `hoanTheoTiLe`, `tinhVi`. Nay `lib/gates.ts` giữ **phần quyết định** của mọi cổng, và `canViewBarn`/`barnViewer` **gọi chung một luật** thay vì là hai bản chép tay (chính cái hình dạng đã đẻ ra §11.37). `tests/cong-quyen.test.ts`: bảng **28 ô** đầy đủ + quét mã nguồn bắt trang/action quên cổng. **Tìm ra một lỗi thật ngay lần chạy đầu**: `traCuuChuTaiKhoan` để trần, ai cũng bắn được, tiêu **khoá VietQR của nông trại** và biến nó thành máy tra tên chủ tài khoản cho người lạ (§11.39). Kèm: `middleware.ts` nay dùng chung `bocMatKhauBasic` - bản cũ tự bóc bằng `split(":")` nên **cắt cụt mật khẩu có dấu hai chấm**, lệch với `isAdmin()`. 310 → **432** phép kiểm |
| *(đợt này, phần bốn)* | **Ba việc chủ dự án nêu sau khi nghiệm thu O/P/Q (Đợt 12).** ① **Chợ mở cửa MUA cho mọi tài khoản** (§11.40) - `reserveListing` từng đòi người mua sở hữu ≥1 chuồng; cổng đó chặn đúng người vừa lập tài khoản và đang muốn **trả tiền** cho hàng của cô chú nuôi thật, trong khi nó **không** giữ được vòng lặp nhận nuôi (75 ngày + vài trăm nghìn không bị thay thế bởi một lần chuyển khoản). Phía **bán** không đổi và không cần cổng: `listLot` đòi một `HarvestLot`, mà lô chỉ ra từ chuồng đang nuôi. **§9.29 đã sửa lại**, đọc trước khi đụng. ② **Chuồng đã hoàn trả biến mất khỏi cổng nông dân** (§11.41) - `returnBarn` giữ nguyên `workerId` nên chuồng vẫn nằm trong danh sách cô chú: không việc, không tin, chỉ là rác che mất chuồng đang cần chăm. Nay lọc `ownerId: { not: null }` ở **cả bốn** truy vấn của `/nong-trai` + đá ở trang chi tiết. **Lọc chứ không xoá `workerId`** - giao lại chuồng cho chủ mới là nó hiện lại đúng cô chú cũ. ③ **`/admin` xoá hẳn được một chuồng** (§11.42) - thao tác **phá huỷ duy nhất** của cả sản phẩm. Ba lớp chắn: `isAdmin()` · gõ lại slug kiểm ở **server** · **từ chối khi lô còn tin đăng đang giữ tiền** (`Payout` cascade theo lô). **Tiền không xoá theo**: `Refund` `SetNull` + `barnLabel` chụp sẵn · `Reservation` gỡ khỏi chuồng rồi `CANCELLED` (chứng từ chuyển khoản thật) · chuồng còn chủ thì ghi khoản hoàn tiền nuôi **trong cùng transaction**. **DB không đổi** - không thêm bảng, không thêm cột. 432 → **433** phép kiểm |
| *(đợt này, phần năm)* | **Giao hàng: vùng · phí · giỏ hàng (Đợt 13, §11.12).** Hai commit. ① **Vùng giao** (§11.43): bảng `DeliveryZone` sửa ở `/admin`, `Address` thêm `zoneId`, **ô CHỌN** khu vực chứ không đoán từ chuỗi địa chỉ. Freeship Hà Nội, tỉnh khác có phí - chủ dự án chốt. ⚠️ **Phí là của MỘT CHUYẾN, không phải một lô**: mọi lô đều đang nằm ở nông trại (§9.29) nên mua 5 lô vẫn là một chuyến xe. ② **Giỏ hàng** (§11.45): `MarketOrder` thay chỗ của "mỗi tin đăng một mã chuyển khoản" - mua 3 lô trước đây là **ba lần chuyển khoản** với ba nội dung khác nhau. Trạng thái `OPEN` chính là cái giỏ; lô trong giỏ đã `RESERVED` nên giữ nguyên luật 24 giờ, không đẻ ra khái niệm giữ-chỗ thứ hai. ③ **Vá một lỗ có sẵn** (§11.44): `confirmMarketPaid` gộp việc `DELIVER` theo **chuồng**, nên hai người mua khác nhau cùng một chuồng dùng chung một việc - một tấm ảnh đóng cả hai đơn và sinh **cả hai `Payout`**, người thứ hai chưa nhận gì mà tiền đã rời ký quỹ. Nay `BarnTask.orderId @unique` + `completeTask` lọc theo đơn. **DB thật đã đổi**: `CREATE TABLE "DeliveryZone"` · `Address.zoneId` · `CREATE TYPE "MarketOrderStatus"` · `CREATE TABLE "MarketOrder"` · `MarketListing.orderId` · `BarnTask.orderId` (unique - **cần `--accept-data-loss`**, đã kiểm cột chưa tồn tại trước khi gõ). **Đã chuyển tiếp dữ liệu thật**: 1 tin đăng `PAID` → một `MarketOrder` giữ nguyên mã cũ, và 1 việc `DELIVER` đang mở → gắn vào đúng đơn đó; kiểm lại còn **0** dòng chưa chuyển. 448 → **450** phép kiểm |
| *(đợt này, phần sáu)* | **Giỏ lên thanh điều hướng, địa chỉ thoát khỏi trang chuồng (Đợt 14, §11.46).** Chủ dự án nêu hai điều, và đào ra một **ngõ cụt hoàn chỉnh** nằm dưới cả hai: `AddressForm` chỉ được vẽ ở `/chuong/[id]/thu-hoach` - trang đòi sở hữu chuồng. Từ Đợt 12 người mua có thể **không có chuồng nào**, nên họ bấm "Bỏ vào giỏ" (được), đọc *"Điền địa chỉ nhận hàng trước rồi mới đặt được nhé"*, rồi **không có một trang nào trong cả sản phẩm để điền**. Câu chữ đúng, đường đi không tồn tại. ① Trang mới **`/cho/gio`** vẽ chính ô địa chỉ đó + thẻ giỏ; mục **🧺 Giỏ hàng** có huy hiệu số nằm thường trực dưới "Nhận chuồng". ② **Cổng địa chỉ dời từ `chotGio` sang `themVaoGio`** - vì vào giỏ **là giữ chỗ thật**, bản cũ để người chưa có địa chỉ rút lô khỏi chợ 24 giờ rồi mới báo họ không đặt nổi; `boKhoiGio` **cố ý không gác** (khoá đường lùi là nhốt lô của người bán). ③ ⚠️ **Thanh điều hướng chỉ sống ở laptop** (`.side-nav` ẩn dưới `lg`) nên `/cho` phải giữ một dòng tóm tắt - dời thẻ giỏ đi mà không để dấu vết là cắt đường về của chính người vừa bỏ hàng vào. ④ Sửa kèm bẫy cũ: `path.startsWith("/cho")` khớp luôn `/chuong` nên mở chuồng thì "Chợ nông trại" cũng sáng. **DB không đổi.** Dọn luôn 4 chỗ CODEMAP còn gọi `reserveListing` (mất tên từ Đợt 13) và §7.9 còn vẽ `upsertTask` theo chuồng (đã sửa ở Đợt 13). 450 → **455** phép kiểm |
| *(đợt này, phần bảy)* | **Giữ chỗ 3 giờ + nút "Tôi đã chuyển khoản" (Đợt 15, §11.48) - và BA LỖ đào ra khi trace (§11.47).** Chủ dự án nêu ba việc: lô người khác giữ phải **hiện ra** trên chợ chứ đừng biến mất; hạn giữ **3 giờ** tính từ lúc vào giỏ tới lúc trả tiền xong; và có nút xác nhận đã chuyển khoản như cọc chuồng. Làm xong cả ba, thêm bất biến **§9.34**. ⚠️ **Ba lỗ có sẵn tìm thấy khi trace, cả ba đều qua bốn đèn xanh:** ① `/api/thanh-toan` nhánh chợ tra `MarketListing.payCode` trong khi mã đã dời sang `MarketOrder` từ Đợt 13 ⟹ luôn 404, `usePayWatch` nuốt im lặng, **ô chuyển khoản của chợ chết câm hai đợt**; ② `confirmMarketPaid` **chỉ webhook gọi được** - mà webhook là tuỳ chọn, nên nông trại chưa nối nó thì tiền đơn chợ về mà không ai xác nhận được (nay có bàn 🧺 ở `/admin`); ③ `releaseStaleHolds` viết trước khi có `MarketOrder` nên nhả lô mà để đơn `RESERVED` với `payCode` còn sống. ⚠️ **Suýt làm sai một chỗ:** đã định ẩn nút "Tôi đã chuyển khoản" khi quá hạn - đó đúng là lúc nó cứu người vừa chuyển tiền. **DB thật đã đổi** (thuần additive): `ALTER TYPE "MarketOrderStatus" ADD VALUE 'REPORTED'` · `MarketOrder.reportedAt`. 455 → **483** phép kiểm |
| *(đợt này, phần tám)* | **Không ai phải chờ trong im lặng (Đợt 16, §11.49).** Đo trước khi làm: `lib/jobs.ts` nhắc `Refund` **0 lần**, `Payout` **0 lần** - tức toàn bộ tiền nông trại đang nợ người dùng, cả hai chiều, đều không có gì tự nhắc. Nay: cron soi cả hai bảng và gõ cửa quản trị (`lib/hang-doi.ts`, mốc 24h cho hoàn tiền / 48h cho chi trả, **một chuông mỗi loại** chứ không mỗi khoản) · người chờ đọc được **đã chờ bao lâu** ở cả ba mặt · hàng đợi `/admin` tự lộ khoản treo lâu bằng dấu ⏰ đỏ · **người mua tự huỷ được đơn chưa trả tiền** (`huyDon`, chỉ `RESERVED`) · giỏ rỗng bỏ quên được dọn. ⚠️ **Luật câu chữ:** không bao giờ hứa một ngày cụ thể - chi trả do người thật làm, nên hệ thống chỉ được **thừa nhận thời gian đã trôi**. 🔴 **Lỗi thật tìm ra khi thử tay:** `chotGio` không có `orderBy` trong khi `gioDangMo` lấy giỏ cũ nhất ⟹ tài khoản có hai giỏ `OPEN` thì bỏ hàng vào xong bấm "Chốt đơn" nhận được *"Giỏ của bạn đang trống"*. ⚠️ **Chưa chạy end-to-end phần gửi chuông** (nằm trong `remindStuff`, gọi nó là chạy cả bảy việc trên DB thật) - đã kiểm câu `WHERE` chọn đúng dòng, gồm hai ca âm. **DB không đổi.** 483 → **506** phép kiểm |
| `bb50471` `7f06c40` | **Hàng rào tần suất (Đợt 17, §11.50).** Đo trước khi làm: `messages.sendingBlocked` là hàng rào **duy nhất** của cả repo, và nó chỉ đếm được vì mỗi tin nhắn tự nó là một dòng trong DB. Bốn cửa còn lại không để lại dòng nào để mà đếm - hai đường gửi OTP (**mỗi lượt là một email Resend thật**), `traCuuChuTaiKhoan` (**lượt gọi VietQR có phí**) và `login` (mật khẩu dò được không giới hạn). Nay có bảng `RateLimit` + `lib/nhip.ts` / `lib/nhip-meta.ts`, bất biến **§9.35**. ⚠️ **Ba chỗ dễ tưởng là có hàng rào mà thật ra không:** ① **`OTP_RESEND_COOLDOWN_MS` không phải hàng rào** - nó khoá theo `(email, purpose)`, mà email là thứ người gọi tự bịa vô hạn; ② **đặt sai CHỖ thì vô hiệu** - hai cửa gửi mã trả lời thẳng rằng email đã có tài khoản hay chưa, đặt bộ đếm *sau* phép tra đó thì mọi lượt bị chặn sớm không được đếm (nên bộ kiểm quét **vị trí**, không chỉ sự tồn tại); ③ **`x-forwarded-for` là header người gọi TỰ ĐẶT ĐƯỢC** - Vercel nối thêm chứ không xoá, nên phải đọc `x-vercel-forwarded-for` / `x-real-ip` trước. Phép cộng và phép so nằm **chung một câu `INSERT … ON CONFLICT`**: đọc-rồi-ghi thì hàng trăm lượt song song cùng đọc số cũ và cùng đi lọt. **Đã đo hai tầng.** DB: 40 lượt song song ⟹ bộ đếm **đúng 40**, đúng 20 lượt lọt. HTTP trên bản đã deploy: `ipHienTai()` **đọc được địa chỉ IPv4 thật** (mệnh đề không kiểm được ở máy) · **12 email KHÁC NHAU ⟹ đúng 10 lọt, lượt 11 chặn** - chính là lỗ của bản cũ · email 5/6 · đăng nhập 10/11. Cả ba phép chạy bằng đường **không gửi email nào, không tạo phiên nào**; đã dọn sạch bộ đếm. **DB thật đã đổi** (thuần additive): `CREATE TABLE "RateLimit"`. 506 → **537** phép kiểm |
| *(đợt này)* | **ChicChic Gia đình - Epic 5: khu khám phá của bé (§9.40, §11.51).** Ba trang `/be/**` · bốn cơ chế học vẽ ra màn hình · ba hành động chuyển trạng thái · cổng ra hỏi mật khẩu; 33 phép kiểm. ⚠️ **Bốn quyết định đáng nhớ:** ① **Thanh điều hướng người lớn nằm ở `app/layout.tsx`, không nằm trong `/be`** - và đó là lỗi đã xảy ra thật: bốn phép kiểm quét `/be/**` đều xanh, `tsc`/`lint`/`build` đều xanh, mà màn hình của đứa trẻ vẫn có SideNav với "Chuồng của tôi · Chợ · Giỏ hàng · Tài khoản". Sửa bằng `HEADER_KHU_BE` từ middleware + lớp bọc **trả về sớm** một khung trần. **Quét mã nguồn không thấy được thứ do lớp bọc vẽ ra - phải mở trình duyệt.** ② **Cổng ra là mật khẩu và KHÔNG đóng dấu `Session.reauthAt`** - dùng chung dấu với `xacMinhLai` nghĩa là mỗi lần thoát khu của bé sẽ âm thầm mở 10 phút cho ba việc nhạy cảm nhất. Cố ý không dựng mã PIN: một bí mật mới phải băm và bảo vệ, đổi lại chút tiện cho một việc hiếm. ③ **Trùng thì tử tế, không phải lỗi**: hai tab cùng bấm xong ⟹ một tab "Giỏi lắm 🎉", tab kia "Bé làm xong bài này rồi 🎉", **cả hai `ok`**. Một câu đỏ ở đây rơi vào mắt một đứa trẻ. ④ **Không nút chết**: nút "Vào khu của bé" chỉ hiện khi bé thật sự gắn với một chuồng đang chạy - lỗi thứ hai vấp phải ở đợt này. **Đã đo trên máy chủ thật + DB thật** (đã dọn sạch): 0 link ra ngoài · mở bài của bé khác bằng URL ⟹ `not-found` · khoá bịa gửi kèm bị loại · hai tab song song ⟹ đúng một lần hoàn thành · ⭐ **rút consent ⟹ ba trang `not-found`, 0 lần hiện tên bé, ba hành động từ chối** · cờ tắt ⟹ tất cả đóng. Thử ngược 5 ca. **DB thật đã đổi** (thuần additive): `LearningMoment.missionDoneAt`. 728 → **778** phép kiểm |
| `3591e6d` | **ChicChic Gia đình - Epic 4: nội dung có kiểu + materializer (§9.39, §11.51).** Catalog 12 đơn vị (6 chương × 2 nhóm tuổi) có kiểu và bộ kiểm đọc được · `LearningMoment` + `LearningEventReceipt` · `dungKhoanhKhac` (idempotent) · nút đồng bộ cho cha mẹ + một việc nền ban đêm · khối chẩn đoán ở `/admin`; 48 phép kiểm. ⚠️ **Bốn quyết định đáng nhớ:** ① **Không con số nào về đàn gà viết cứng trong nội dung** - số trứng tới từ `factSnapshot` của chính sự kiện. Viết "hôm nay được 12 quả" vào catalog nghĩa là mọi đứa trẻ ở mọi chuồng đọc đúng con số đó, tức nói dối một đứa trẻ về chính đàn gà của nó. ② **Ghi lô hằng ngày CỐ Ý không sinh bài.** Nông dân ghi lô mỗi sáng; mỗi lô một bài là đứa trẻ nhận cùng một bài lặp lại mỗi ngày - vừa nhàm vừa đúng kiểu "kéo trẻ vào app mỗi ngày" mà §8.3 cấm. **Bài gắn với MỐC, không gắn với nhịp.** ③ **Chỉ sự kiện sau `acceptedAt`** - chuồng đã nuôi cả năm trước khi gia đình tham gia, đổ hết quá khứ vào nhật ký của bé là dựng một lịch sử bé chưa từng sống. ④ **`FAILED` không tự thử lại** - hàng đợi xếp theo thời gian nên một sự kiện hỏng ở đầu hàng sẽ chặn mọi sự kiện sau nó mỗi đêm; đổi lại nó phải hiện ở `/admin`. **Đã đo trên máy chủ thật + DB thật** (hai bé hai nhóm tuổi, đã dọn sạch): mỗi bé nhận đúng biến thể nhóm tuổi mình · bài chương 4 mang **số trứng thật** · ⭐ bài chương 5 **không mang `deliverTo`** dù hành động ngay cạnh vừa ghi tên/số điện thoại/địa chỉ · ⭐ **hai lượt đồng bộ song song ⟹ 0 cặp (bé, sự kiện) nhân đôi** · sự kiện cũ hơn `acceptedAt` 30 ngày ⟹ 0 bài · ⭐ **rút consent ⟹ bé đó dừng nhận bài ngay** · cha mẹ khác bấm đồng bộ ⟹ không đổi gì của gia đình này · cờ tắt ⟹ 0 bài mới. Thử ngược 5 ca. **DB thật đã đổi** (thuần additive): `CREATE TYPE LearningMomentStatus` + `LearningReceiptStatus` · `CREATE TABLE LearningMoment` + `LearningEventReceipt`. 679 → **728** phép kiểm |
| `7564bbf` | **ChicChic Gia đình - Epic 3: hộp thư đi `DomainEvent` (§9.38, §11.51).** Một bảng + hai hàm ghi + bảy nơi phát + một khối chẩn đoán chỉ-để-nhìn ở `/admin`; 36 phép kiểm ở `tests/su-kien.test.ts`. ⚠️ **Bốn quyết định đáng nhớ:** ① **`createMany({skipDuplicates})` chứ không `create`** - không phải chuyện gọn gàng: trong Postgres một lỗi khoá trùng **làm hỏng cả transaction đang mở**, nên kiểu "cứ `create` rồi bắt P2002" sẽ kéo theo việc của nông dân quay đầu ở câu lệnh kế tiếp. ② **`payload` lọc theo danh sách trường, và trường lạ bị LOẠI BỎ chứ không ném lỗi** - hướng của hai kiểu hỏng không cân nhau: một dữ kiện thiếu thì vá được, một địa chỉ nhà đã hiện lên trong màn hình của trẻ thì không. Chỗ nguy nhất là `LOT_CLAIMED`/`HANDOVER_COMPLETED`: hành động ngay cạnh đó đang ghi `deliverTo` (tên, số điện thoại, địa chỉ) vào `HarvestLot`. ③ **Sáu trong bảy nguồn ghi trong transaction của việc thật**; ngoại lệ duy nhất là `advanceFlocks` vì `updateMany` không nói đàn nào đã đổi, phải đọc lại mới biết - bộ kiểm chốt cứng con số một đó. ④ **Cờ tổng tắt ⟹ không ghi dòng nào** - lõi nông trại không gánh thêm phép ghi cho tính năng đang tắt; đổi lại là quãng cờ tắt sẽ không có bài học, chấp nhận được vì materializer vốn chỉ lấy sự kiện **sau `acceptedAt`**. **Đã đo trên máy chủ thật + DB thật** (bộ dữ liệu dùng-một-lần, đã dọn sạch): sáu loại sinh đủ và đúng khoá qua hành động thật · ⭐ `LOT_CLAIMED` **không mang `deliverTo`** · ghi lại cùng khoá ⟹ **0 dòng, không lỗi, câu lệnh kế tiếp vẫn chạy** · transaction ném lỗi ⟹ **0 dòng** · ⭐ **cờ tắt ⟹ việc nông dân vẫn xong, không sinh dòng nào**. `FLOCK_STAGE_CHANGED` **chưa chạy thật** (nằm trong `runDailyJobs` - cấm chạy trên DB thật). Thử ngược 4 ca, mỗi ca đỏ đúng phép kiểm của nó. **DB thật đã đổi** (thuần additive): `CREATE TYPE DomainEventType` · `CREATE TABLE DomainEvent`. 643 → **679** phép kiểm |
| `96be2c7` `86e6320` | **ChicChic Gia đình - Epic 0 + Epic 1 (§11.51).** Trụ thứ tư của sản phẩm, làm theo `CHICCHIC-FAMILY-LEARNING-SPEC-v1.1-FINALIZATION.md` (đọc trước, thắng khi xung đột) + `CHICCHIC-NEXT-PLAN-FAMILY-LEARNING.md`. **Epic 0**: cờ tổng `FAMILY_LEARNING_ENABLED` - không đặt gì ⟹ **TẮT**, và **không có ngoại lệ cho dev** (ngược `laQuanTri`, cùng hướng §9.20); **không** phải `NEXT_PUBLIC_` vì biến đó bị nướng vào bundle, tức tắt tính năng mà vẫn khoe rằng nó tồn tại; đọc mỗi lần gọi để đổi biến rồi restart là ăn ngay. Kèm dọn drift `HUONG-DAN` (spec §2.3 bắt buộc): §I2 còn ghi *"người mua phải đang nuôi ≥1 chuồng"* - cổng đó gỡ từ **Đợt 12**, và §I4 còn bảo chủ dự án đi nghiệm thu *"nút mua phải bị khoá"*, tức là sai tới mức **dạy người ta xác nhận một cái cổng đã không còn**; "giữ chỗ 24h" ở bốn chỗ (thật là 3 giờ từ Đợt 15); bảng J5 thiếu hai lời nhắc của Đợt 16. **Epic 1**: `FlockLifecyclePolicy` + `FamilyEnrollment` + bất biến **§9.36** - đàn đã hứa với một gia đình **chỉ còn `RETIRE`**. ⚠️ **Ba quyết định đáng nhớ:** ① cột chính sách chụp lên **`Flock`**, không lên hồ sơ trẻ - cha mẹ xoá dữ liệu con thì cam kết với đàn gà thật **vẫn còn**; ② **lời mời KHÔNG khoá gì cả**, khoá chỉ xảy ra khi cha mẹ đồng ý rõ ràng (Epic 2) - khoá lúc mời là quyết định thay người khác về số phận một đàn gà thật dựa trên cái bấm của người thứ ba; ③ chốt "một chuồng một suất" là cột **`barnLiveKey` unique** dưới DB (nhiều `NULL` được phép), không phải `if` rồi `create` - Prisma không khai được partial unique index. **Đã đo trên máy chủ thật + DB thật (15 phép, đã dọn sạch):** bắn thẳng `choice=MEAT` vào action bằng phiên chủ chuồng ⟹ **0 dòng `LifecycleDecision`**, `stage` không đổi, 5 con vẫn `ALIVE`; `RETIRE` thì qua · **8 lệnh `INSERT` song song ⟹ 1 qua, 7 dính `P2002`** · mời xong `Flock` vẫn `STANDARD` · ⭐ **cờ tắt ⟹ `/admin` render 177KB mà 0 lần nhắc tới Gia đình**. Thử ngược 3 ca, mỗi ca đỏ đúng phép kiểm của nó. **DB thật đã đổi** (thuần additive): `CREATE TYPE FlockLifecyclePolicy` + `FamilyEnrollmentStatus` · `Flock.lifecyclePolicy` (11 đàn backfill `STANDARD`) · `CREATE TABLE FamilyEnrollment`. 537 → **562** phép kiểm |
| `879c6bb` `922d0b5` | **ChicChic Gia đình - Epic 2: hồ sơ trẻ, consent, quyền riêng tư (§9.37, §11.51).** Bốn thứ mới: `ChildProfile` + `ChildConsentEvent` + `ChildBarnLink` + cột `Session.reauthAt`; bốn trang `/gia-dinh` (`·` `/xac-minh` `/tre-moi` `/quyen-rieng-tu`); sáu server action ở `family-actions.ts`; 56 phép kiểm ở `tests/gia-dinh.test.ts`. ⚠️ **Bốn quyết định đáng nhớ:** ① **`Flock.lifecyclePolicy` giờ có đúng MỘT đường ghi** - `nhanLoiMoiGiaDinh` - và nó **một chiều**: rút consent, xoá dữ liệu con, tạm dừng suất, không cái nào đảo được. Cái ghi vào đó không phải một cài đặt mà là lời hứa rằng con gà đứa bé đặt tên sẽ già đi ở nông trại, và nông trại trả bằng thức ăn thật trong nhiều tháng. ② **Xác minh lại = một cột `Session.reauthAt`**, không phải một cơ chế auth mới (đúng ghi chú §C.2 của spec v1.1: dùng lại `verifyPassword` + `lib/nhip`). Nằm trên `Session` chứ không trên `User` - gõ đúng mật khẩu ở máy này không mở cửa cho phiên treo ở máy khác; hiệu lực 10 phút và **tiêu ngay sau khi việc xong**. ③ **Xoá dữ liệu trẻ KHÔNG `delete` dòng `ChildProfile`** - `ChildConsentEvent` cascade từ nó, xoá dòng là xoá luôn cuốn sổ chứng minh mình đã làm đúng (§17.4). Cách làm: bôi trắng `nickname`/`avatarKey`, xoá hẳn `ChildBarnLink`, để lại bia mộ. ④ **Rút ≠ xoá, và giao diện không được gộp hai nút** - người ta thường muốn rút trước, gộp lại nghĩa là có ngày ai đó mất cuốn album của con mình vì tưởng chỉ đang tạm dừng. **Đã đo trên máy chủ thật + hai tài khoản cha mẹ thật + DB thật (30 phép, đã dọn sạch):** ⭐ **cha B đã gõ đúng mật khẩu của chính mình vẫn bị từ chối cả bốn đường** trên dữ liệu của cha A · ⭐ **rút consent rồi xoá sạch dữ liệu con ⟹ đàn VẪN `FAMILY_RETIRE_ONLY`**, hồ sơ còn lại là bia mộ trống mà cuốn sổ consent còn đủ **5 dấu mốc** · nhận lời mời khi bé còn `DRAFT` bị từ chối và đàn vẫn `STANDARD` · `avatarKey=/uploads/be.jpg` bị từ chối · hàng rào tần suất **8 lọt, lượt 9 chặn**, mật khẩu đúng cũng chặn, tài khoản kia không vạ lây · dấu xác minh về `null` ngay sau mỗi việc · ⭐ **cờ tắt ⟹ 4 trang `not-found` và 6 action từ chối**, trang render 21KB với **0 lần** xuất hiện `avatarKey`/`consent`/`tre-moi`. Thử ngược 3 ca, mỗi ca đỏ đúng phép kiểm của nó. **DB thật đã đổi** (thuần additive): `CREATE TYPE ChildAgeBand` + `ChildProfileStatus` + `ConsentAction` · `CREATE TABLE ChildProfile` + `ChildConsentEvent` + `ChildBarnLink` · `Session.reauthAt` (nullable). 562 → **640** phép kiểm |
| `f8081f1` | **Trải nghiệm chờ & trạng thái rỗng (Đợt 8).** Repo có đúng **một** `loading.tsx` ở gốc, mang hình dạng **trang chuồng** - nên mở `/cho`, `/admin`, `/nong-trai` cũng thấy hình một cái chuồng vài giây rồi trang nhảy sang bố cục khác hẳn. Nay **19 khung chờ** dựng từ `components/Skeletons.tsx`, mỗi cái bám bố cục thật của route mình. Kèm ba chỗ khác: nút *"Xác nhận lựa chọn này"* ở màn kết chu kỳ (nút **nặng nhất và chạy lâu nhất** repo) trước đó là `<form action>` trần **không có phản hồi nào** → đổi sang `useTransition` · §11.24 câu ở màn đổi tên hứa tên mới hiện "trên **biển tên treo trước chuồng**" - đúng với hình VẼ, sai với tấm biển gỗ thật ngoài vườn → nói đúng phạm vi + chỉ đường nhắn cô chú · hồ sơ nông dân **chưa có ảnh nào** trước đó im lặng hoàn toàn, đọc ra thành "người này chưa làm gì bao giờ" ngay lúc khách đang chọn ai chăm chuồng mình. Bộ kiểm mới `tests/khung-cho.test.ts` (**bộ duy nhất đọc file nguồn**) 148 → **246** |
| `8ac9742` | **Thu tiền nuôi - lỗ doanh thu lớn nhất của repo (§7.16, §11.13).** Sản phẩm thu **đúng 50.000đ cọc** rồi thôi; toàn bộ `priceEstimateVnd` nằm im như con số ước tính không ai đòi, trong khi nông trại nuôi thật. Nay `BarnInvoice` + `PayKind` thứ năm (`CHICN…`) + webhook + `/admin` + gia hạn. **Gà thịt** một hoá đơn trọn lứa · **gà đẻ** một hoá đơn mỗi tháng (bảng giá in "/ tháng" từ đầu) · **cọc trừ vào kỳ đầu** (đã sửa mọi câu "cọc hoàn lại" cho khỏi nói dối). Phát hành khi chủ chuồng mở trang (`<InvoiceGate>`, **sau** khi render - render không được ghi) **và** trong cron (không mở app không được thành cách trốn tiền). Quá hạn 7 ngày → `<BarnUnpaid>` + `ownedBarn()` từ chối. Bất biến mới **§9.33** |
| `68c8e10` | **Nối nguồn thu - phí nuôi dưỡng đàn nghỉ hưu (§11.13, §7.15).** Màn kết chu kỳ **hứa** *"Phí nuôi dưỡng 60.000đ/tháng, đối soát tay như các khoản khác"* rồi bấm xong thì không có gì: không hoá đơn, không mã chuyển khoản, `/admin` không biết có ai vừa chọn nghỉ hưu. Một dòng `LifecycleDecision.retireFeeVnd = 60000` nằm im không ai đọc. Nay `CareOrder` + trang `/chuong/[id]/nghi-huu` + `PayKind` thứ tư (`CHICR…`) + webhook tự khớp + khối đối soát ở `/admin` + nhắc trước hạn trong cron. **Trả trước theo khối 3/6/12 tháng**, không phải hoá đơn hằng tháng (12 lần chuyển khoản tay/năm cho một đàn là một cỗ máy ma sát), và **không giảm giá theo khối**. Bất biến mới **§9.32** |
| `e6d0f1c` | **Kho ảnh chưa từng chạy được lần nào.** Chủ dự án báo "chọn ảnh JPG mà app kêu sai định dạng" ở cả máy tính lẫn điện thoại. Nguyên nhân: `signUpload` gửi mỗi header `Authorization`, thiếu `apikey` ⟹ với key Supabase **đời mới** (`sb_secret_…`) endpoint ký URL trả `400 Invalid Compact JWS`. Lúc phát hiện, **kho rỗng cả 5 thư mục** - chưa tấm ảnh minh chứng nào từng lên được. Sửa kèm: `signUpload` trả `reason` **tách bạch** (câu báo cũ đổ lỗi cho ảnh của người dùng) · **hai ô chọn file** - `capture` *thay thế* hộp chọn file nên điện thoại không có đường vào thư viện · chặn HEIC tại máy · đặt `Content-Type` lúc PUT · video 25→45MB (trần kho **50MB, đã đo**) |
| `1af07d2` | **Video iPhone: có tiếng mà không có hình.** Chủ dự án tải lên được rồi nhưng video ra ô đen kèm tiếng nói. Soi thẳng hai file trong kho qua HTTP Range: cả hai là **H.265/HEVC** (`hvc1`) + tiếng AAC - Chrome/Windows giải mã được tiếng, không giải mã được hình, và **`onError` không kêu** nên không ai biết. **Cùng gốc với lỗi HEIC**: một công tắc *"High Efficiency"* trên iPhone đẻ ra cả hai. Thêm `lib/video.ts` đọc codec **thật trong file** (đi dọc hộp MP4 tìm `moov` - video iPhone để `moov` ở CUỐI nên không đọc đại vài MB đầu được) rồi chặn lúc chọn; video đã lỡ nằm trong sổ thì trình phát tự nhận ra bằng `videoWidth === 0`. Kèm: ô xem trước cũ nhét URL video vào `<img>` ⟹ **mọi video đều ra biểu tượng ảnh vỡ** (không lộ trên chuồng demo vì seed có sẵn `posterUrl`) |
| `421d16c` | **QR truy xuất thật** - `Illustrations.QRCode` là lưới ô vuông ngẫu nhiên **không mã hoá gì**, nằm đúng trang bán niềm tin; và trang truy xuất lại sau `requireUser` nên **người được tặng - người duy nhất cần kiểm chứng - không xem được**. Nay mỗi lô có `publicCode` + mã QR thật (`lib/qr.ts`) quét ra `/tx/<mã>` **công khai** (§7.14). Ranh giới lộ gì là bất biến mới **§9.31**. Component giả đã **xoá hẳn** |
| `ea46195` | **Lưới an toàn** - `npm test` (vitest, **70 phép kiểm, ~1 giây**, đã vào CI). Mỗi `it` trong `tests/bat-bien.test.ts` khoá **một dòng §9**. Chạy lần đầu đã bắt hai chỗ hành vi lệch với ý định code: `cleanLine({})` ra `"[object Object]"` (biến được thành tên chuồng qua lời gọi ngoài trình duyệt) và `clampQty(null)` rơi về *min* thay vì *mặc định*. ⚠️ **Cố ý không nối DB, không dựng máy chủ** ⟹ **không phủ cổng quyền và không phủ phép ghi DB** - xem CODEMAP §13 trước khi tin vào màu xanh |
| `d96252b` | **Nhận hàng tận nhà** - khép nốt vòng đời. Trước đó một lô chỉ có hai kết cục: bán trên chợ, hoặc `EXPIRED`; người nuôi 5 tháng **không có cách nào nhận trứng của chính mình**. Nay có `Address` + `LotStatus.CLAIMED` + `TaskKind.HANDOVER` (§7.13). `HANDOVER` **tách riêng** khỏi `DELIVER` - gộp thì một tấm ảnh đóng cả hai chuyến và tiền chợ được chi dựa trên ảnh của chuyến khác |
| `5854acd` | **Vòng nhắc** - việc nền thứ 5 (`lib/jobs.remindStuff`), thứ duy nhất trong cron **không đổi dữ liệu, chỉ nói**. Năm chuyện trước nay im lặng tuyệt đối: đàn hết chu kỳ chưa quyết định · **lô sắp hết hạn** (nhắc TRƯỚC, không báo sau) · việc nằm im quá lâu → nhắc **nông dân**, không mách chủ chuồng · hoá đơn `REPORTED` chưa đối soát · chuồng có nông dân tạm dừng. Bảng `Nudge` là chốt **"nhắc một lần, không nhắc mỗi ngày"** |
| `7a7cb7c` | **Khép hai mắt xích hở.** ① `TaskKind.HARVEST` - chọn "nhận thịt" nay **giao việc thật** cho nông dân, và việc đó **không tích xong được khi sổ thu hoạch còn trống** (§7.11). ② `admin-actions.reassignBarn` + khối "🔄 Chuồng đang không có người chăm" ở `/admin` - bàn giao chuồng của cô/chú đang tạm dừng, **kèm cả việc đang treo** (§7.12) |
| `d8108c7` | **Khép lứa gà thịt** - `/ket-chu-ky` mở cho cả hai dòng (trước chỉ gà đẻ, nuôi trọn lứa gà thịt xong không ai hỏi gì) · sửa nhánh `RENEW` đang làm hỏng dữ liệu (5 con cứng, đặt thẳng `LAYING`, giữ `vaccinatedAt` cũ) |
| `b7756d2` | **Việc nền theo ngày** (`GET /api/cron`, Vercel Cron) - job nền **đầu tiên** của repo: đàn gà lớn lên · nhả chỗ giữ trên chợ · đóng sổ lô quá hạn · huỷ hoá đơn trang trí bỏ quên |

Chi tiết nghiệp vụ của cron nằm ở **CODEMAP §7.10**, bất biến kèm theo ở **§9.30** và **§9.8** (chống dội chuông).
Hai vòng lặp mới ở **§7.11** và **§7.12**.

**DB thật đã đổi** (mọi thứ đều additive - xem trước bằng `prisma migrate diff --script` rồi mới `db push`):
`ALTER TYPE "TaskKind"` thêm `HARVEST` rồi `HANDOVER` · `ALTER TYPE "LotStatus"` thêm `CLAIMED` ·
`CREATE TABLE "Nudge"` · `CREATE TABLE "Address"` · `CREATE TABLE "CareOrder"` · `CREATE TABLE "BarnInvoice"` · `HarvestLot` thêm `claimedAt` + `deliverTo` + `publicCode`.

⚠️ Riêng `publicCode` (cột **unique**) đòi `--accept-data-loss` - đúng bẫy §10. Quy trình đã theo: xác minh cột **chưa tồn tại** và bảng **0 dòng** rồi mới chấp nhận. Lần sau gặp lại thì kiểm y như vậy, đừng gõ cờ đó theo phản xạ.

---

## 2b. Kế hoạch đang chạy

Đã làm xong: **① vòng nhắc ✅ → ② nhận hàng tận nhà ✅ → ③ lưới an toàn ✅ → ④ QR truy xuất thật ✅ → ⑤ sửa kho ảnh + video HEVC ✅ → ⑥ nối nguồn thu ✅ → ⑦ thu tiền nuôi + khoá chuồng ✅ → ⑧ trải nghiệm chờ ✅ → ⑨ chín mục chủ dự án nêu ✅ → ⑩ đường hoàn tiền + lứa mới hết miễn phí ✅ → ⑪ bộ kiểm phủ cổng quyền ✅ → ⑫ chợ mở · dọn chuồng · xoá chuồng ✅**.

> ✅ **Chủ dự án đã nghiệm thu O, P, Q trên trình duyệt thật (2026-08-11)** - tức đợt 8, 9, 10 và phần lứa mới đều đã được bấm tay và chạy đúng như kỳ vọng. Còn nợ đúng **mục R** (đợt 12).

Kèm trong đợt ⑤: **header an ninh** (`next.config.mjs`). Quét production thấy Vercel chỉ tự đặt `Strict-Transport-Security`, còn `X-Frame-Options` `X-Content-Type-Options` `Referrer-Policy` `Permissions-Policy` đều trống - app này có nút bấm-một-cái-là-xác-nhận-tiền nên không chắn iframe là mời clickjacking. **CSP cố ý chưa làm** (§11.32): đặt sai là trắng trang, phải có người soi console trình duyệt.

**Đợt tiếp theo, xếp theo giá trị** (chi tiết ở §4): **phí giao hàng + giới hạn khoảng cách** (§11.12) · **siết an ninh và đối soát** (§11.19 §11.21 §11.32 - CSP nằm ở đây) · hộp thư & thông báo (§11.6 §11.20) · test phủ cổng quyền (§11.18).

> ✅ **Đợt ⑩ đã chạy thử THẬT trên DB thật**: **38 đạt / 0 hỏng** qua route tạm + phiên thật, gồm cả phép âm tính (gõ sai câu xác nhận · người khác hoàn trả chuồng của mình → **không đổi gì** · chưa đăng nhập · không phải người mua · ngoài cửa sổ 3 ngày · bấm hai lần → vẫn một dòng nợ · không phải admin · từ chối mà không ghi lý do · đóng sổ trước khi duyệt · gõ thừa số 0 → chặn · đóng sổ hai lần → chặn). Riêng lỗ rò kiểm thêm **33 phép** trên cả 3 chuồng thật đã hoàn trả (đều đóng) **và** 3 chuồng trưng bày (đều còn mở - không phá đồ Đợt 9). Dữ liệu tạm đã dọn, kiểm lại `Refund` toàn bảng = 0.

> ✅ **Phần lứa mới cũng đã chạy thử THẬT**: **26 đạt / 0 hỏng** (người lạ bấm → **không đổi gì** · chưa đăng nhập → không đổi gì · chưa qua 1 ngày → chưa phát hoá đơn · lứa mới gà thịt **đẻ ra hoá đơn seq 2 trọn giá, không trừ cọc lần hai, chưa quá hạn lúc vừa sinh** · gọi lại 3 lần vẫn đúng 2 hoá đơn · bấm lứa mới lần hai khi đàn đang `BROODING` → chặn · gà đẻ **chỉ thêm một kỳ**, không truy thu 60 ngày nằm chờ · không đụng hoá đơn chuồng nào khác) + **6 phép** trên màn kết chu kỳ (giá 500.000đ/lứa hiện đúng trước khi bấm). Dữ liệu tạm đã dọn, `billingFrom` toàn bảng = 0.

> ✅ **Đợt ⑪ đã nghiệm thu hai lớp.** ① **Thử ngược 7 ca** - tái lập lỗ rò `!ownerId`, đảo thứ tự nhánh, gỡ cổng khỏi một trang, thêm action không kiểm quyền, cho production đi qua khi thiếu `ADMIN_PASSWORD`, cho quản trị đọc hộp thư sạch, cho nông dân tạm dừng vào lại - **cả 7 đều làm bộ kiểm đỏ** đúng chỗ. ② **28 đạt / 0 hỏng** trên máy chủ thật + phiên thật, để chắc bản refactor **không đổi hành vi**: 6 loại người × chuồng riêng tư · chuồng trưng bày vẫn mở · chuồng hoàn trả đóng với tất cả trừ nông dân phụ trách · ba trang có thao tác · bốn phép hộp thư · năm phép `/admin` · hai phép cổng nông dân. Dữ liệu tạm đã dọn - **kể cả hai phiên tôi tạo cho tài khoản nông dân THẬT** (`chutam`, `anhdung`), đó là token đăng nhập sống, phải xoá tay vì chúng không nằm trong nhóm `ZZTMP`.

> ✅ **Đợt ⑬ đã chạy thử THẬT trên DB thật: 39 đạt / 0 hỏng** (vùng giao) **+ 54 đạt / 0 hỏng** (giỏ hàng), gồm nhiều phép âm tính (thiếu vùng · `zoneId` bịa · chưa đăng nhập · không phải admin · trùng tên vùng · phí âm · phí vượt trần · vùng đã tắt · nông dân mua · mua lô của chính mình · đoạt lô đang trong giỏ người khác · chốt giỏ hai lần · rút lẻ sau khi đã chốt · xác nhận tiền hai lần). Ba phép có dấu ⭐ là phần đáng tin nhất: **phí giao đúng một lần cho hai lô của hai người bán khác nhau** · **hai người mua cùng một chuồng có hai việc giao riêng** · **tích việc của người A không đụng gì tới đơn và `Payout` của người B**.

> 📌 **Một phép đo của TÔI sai ở đợt ⑬** - lại đúng kiểu cũ nhưng ở dạng khác: giàn thử gọi `resolvePayCode(code)` một tham số trong khi hàm nhận `(kind, code)`, nên `kind` thành chuỗi mã và `code` thành `undefined` → rơi xuống nhánh DECOR và ném lỗi Prisma. Nhìn màn hình thì giống hệt "mã chợ tra không ra đơn". Bài học: **giàn thử phải đi đúng đường mà mã thật đi** - webhook bóc mã bằng `parsePayCode` trước rồi mới tra, và lúc giàn thử làm y như vậy thì đạt ngay.

> ✅ **Đợt ⑫ đã chạy thử THẬT trên DB thật: 47 đạt / 0 hỏng** qua route tạm + phiên thật + trang thật, gồm **6 phép âm tính** (tài khoản nông dân mua → chặn · mua lô của chính mình → chặn · chưa đăng nhập → chặn · không phải admin thì không xoá được · gõ sai slug → chặn · chuồng còn đơn chợ **đã trả tiền** → chặn và chuồng còn nguyên). Phép quan trọng nhất là **thứ SỐNG SÓT sau khi xoá**: đếm lại 11 bảng con đều về 0, nhưng `Refund` còn nguyên với `barnId = null` + `barnLabel` + 200.000đ ở `REQUESTED`, `Reservation` còn nguyên ở `CANCELLED` với `payCode`, và có `Event("barn_deleted")` chép sẵn mọi con số. Đã thử thêm nhánh **chuồng chưa có chủ** (xoá được, không sinh khoản hoàn nào). Dữ liệu tạm đã dọn hết.

> 📌 **Ba phép đo của TÔI sai trong đợt ⑫, không phải code sai** - cả ba cùng một kiểu: **đo bằng chuỗi hiển thị thay vì bằng khoá.** ① đếm tiêu đề việc để tìm chuồng đã ẩn, mà ba chuồng thử đều có việc **trùng tên** → phải đếm bằng **slug**. ② tìm đơn cọc bằng `payCode.includes(...)` khớp nhầm chuồng khác → phải khớp phần đuôi. ③ báo động vì nhãn chuồng vừa xoá **vẫn còn trên `/tai-khoan`** - hoá ra nó nằm trong dòng *"Tiền nuôi chưa dùng hết · <tên chuồng>"*, tức **đúng thứ tôi vừa cố tình giữ lại**. Bài học: khi phép kiểm đỏ ở chỗ mình vừa viết cẩn thận, **soi ngữ cảnh quanh chuỗi đó trước khi sửa code**.

> 🧹 **Dọn dẹp bắt được rác của HAI đợt trước:** ba `Notification` mang tiền tố `ZZTMP-HOAN` và `ZZTMP-LUA` (đợt 10) vẫn nằm trong hộp chuông của một tài khoản **thật**. Script dọn cũ lọc theo `userId` của người dùng tạm nên không thấy chúng - chuông thì gửi cho **người nhận thật**. Lần sau dọn xong nhớ quét thêm `Notification.title` theo tiền tố.

> 📌 **Hai lần phép đo của TÔI sai trong đợt ⑪, không phải code sai** - cùng một bài học: ① `grep "Tests.*failed"` trả về rỗng vì **mã màu ANSI** chen giữa chữ và số, nên bảy ca thử ngược đều báo "xanh" trong khi thật ra đều đỏ; phải lọc mã màu ANSI khỏi output trước khi grep. ② Kỳ vọng "phiên `role = ADMIN` mở được `/admin`" là **sai từ đầu**: middleware chỉ biết Basic Auth, không biết phiên - đúng thiết kế, có từ trước. Cả hai lần, phản xạ đúng là **soi output thô một ca bằng tay** trước khi tin con số tổng.

> 📌 **Một chỗ phép kiểm của TÔI sai, không phải code sai:** ba phép đầu ở nhóm lỗ rò báo HONG vì tôi đòi HTTP 3xx. Next **streaming** đã đẩy vỏ trang đi trước, nên `redirect()` thành lệnh phía client và mã trạng thái vẫn là 200 - đúng thứ đã ghi ở §11 từ đợt trước. Kiểm lại bằng **nội dung** (tên chuồng không có trong HTML, có lệnh chuyển sang `/dang-nhap`) thì đạt. Bài học: ở repo này **đừng bao giờ nghiệm thu cổng quyền bằng mã trạng thái**.

> ✅ **Đợt ⑧ đã đo THẬT trên bản build production** (`npx next start`, phiên thật rồi xoá): khung chờ ra **trước** nội dung thật trong cùng một phản hồi (`/nong-dan/<id>`: khung ở byte 3.244, nội dung ở byte 14.747) · mỗi route ra **số ô khung khác nhau** (chuồng 14 · chợ 7 · tài khoản 5 · truy xuất 4) tức đúng khung riêng chứ không phải khung chung · màn rỗng nông dân mới hiện đúng với dữ liệu thật (Chị Hoa, 0 ảnh). **Chưa kiểm được bằng mắt**: có bị giật khi trang thật hiện ra không, và nút *"Đang gửi tới nông trại…"* (chỉ hiện sau khi bấm "Chọn" nên curl không thấy). Checklist ở `HUONG-DAN-SETUP-DEPLOY` mục **O**.

> 📌 **Một chỗ tôi suýt làm sai trong đợt ⑧, đáng nhớ:** đã viết `useFormStatus` với lý lẽ *"giữ `<form action={…}>` cho trang bấm được khi JavaScript chưa tải xong"*. Lý lẽ đó **sai** - cái sheet chứa form chỉ mở ra bằng `onClick`, không có JS thì không ai tới được nó. Và `useFormStatus` là hook duy nhất trong repo **không kiểm được bằng curl** (nút chỉ tồn tại sau một cú bấm). Đã đổi về `useTransition`, đúng cách mọi nút khác trong repo đang làm. Bài học: một lập luận nghe hợp lý về "progressive enhancement" thì phải kiểm xem đường đi đó **có thật sự tồn tại** không.

> ✅ **Ảnh đã chạy** - chủ dự án xác nhận tải lên và xem lại được. Video tải lên được nhưng ra ô đen (phần 2 ở bảng trên).

> ✅ **Đợt ⑥ đã chạy thử THẬT trên DB thật**: 38 phép qua route tạm (cổng quyền, tiền tính ở server, idempotent, mua nối tiếp, huỷ) + 14 phép qua webhook SePay (sai khoá → 401, chuyển thiếu → MISMATCH không xác nhận, đủ tiền → MATCHED, gửi lại 7 lần không cộng thêm kỳ). Dữ liệu tạm đã dọn, kiểm lại `CareOrder` toàn bảng = 0.

> ⚠️ **Còn nợ nghiệm thu trên máy thật:** chặn HEIC, chặn HEVC, hai nút chọn file và ô xem trước video **đều là mã chạy trong trình duyệt** - không có trình duyệt nào trong tay để tự bấm. Checklist ở `HUONG-DAN-SETUP-DEPLOY` mục **L**. Riêng phần đọc codec thì đã chạy `soiVideo` lên **chính hai file iPhone thật** trong kho: nhận đúng `hvc1` trong 0–1ms, không gắn cờ nhầm ảnh JPG.

> 📌 **Hai bài học đắt nhất, đừng quên:**
> 1. `tsc` + `lint` + `npm test` + `build` **xanh hết** trong khi một tính năng chính chết câm. Bộ kiểm **không nối mạng** nên mọi biên giới với dịch vụ ngoài đều là vùng mù. Phép kiểm định kỳ có nối mạng: vẫn chưa có (§11.4).
> 2. **Chuồng demo có dữ liệu seed đầy đủ hơn dữ liệu thật** - `posterUrl` có sẵn ở seed nên lỗi "video ra ảnh vỡ" không bao giờ lộ khi thử trên demo. Thử tính năng media thì phải thử bằng **thứ mình vừa tải lên**, không phải thứ seed dựng sẵn.

---

## 2c. Đợt 9 - chín mục chủ dự án nêu

| # | Việc | Đã làm gì |
|---|---|---|
| 1 | Khách chưa đăng nhập xem thử chuồng | Nới **§9.5** qua `lib/auth.barnViewer`: ba trang chỉ-để-xem (chuồng · nhật ký ảnh · truy xuất) mở cho khách **khi `isPublic`**. Trước đó `prisma/seed.ts` đã ghi *"3 chuồng seed là chuồng trưng bày - khách chưa đăng nhập vẫn xem được"* nhưng `canViewBarn` gọi `requireUser` trước nên ý đó **chưa bao giờ chạy** |
| 2 | Gà thịt cập nhật cân nặng theo tuần | Bảng `WeighIn` + `TaskKind.WEIGH` + cron hẹn mỗi tuần + biểu đồ cột trên trang chuồng. **Không bịa số**: tuần chưa cân thì để trống |
| 3 | Ẩn "nhận thêm chuồng" khi đã có chuồng | Bỏ khỏi thanh điều hướng, danh sách chuồng, lưới lối tắt. **Giữ đúng một lối** ở `/tai-khoan` (§11.34) |
| 4 | Bốn dòng cam kết ở trang chủ phải bấm được | `lib/showcase.loiVaoChuong` → vào chuồng **của chính họ** nếu có, không thì chuồng trưng bày |
| 5 | Sửa được tên gà sau khi nhận chuồng | `actions.renameBird` + nút ✎ ngay tại dòng của con đó. Bỏ tên cũng là lựa chọn thật |
| 6 | Đăng bán mà chưa có tài khoản → dẫn tới ô điền | Ô điền mở **ngay tại chỗ**, dưới đúng cái lô đang muốn bán |
| 7 | Người bán nhận tiền và rút được | Ví ở `/cho/cua-toi` (rút được · đang giữ hộ · lỗi) + nút rút + `/admin` xếp người đã xin rút lên trước. ⚠️ **Giữ nguyên ký quỹ §9.29** - xem ô cảnh báo dưới bảng |
| 8 | Cấp đông cho chính mình | `TaskKind.FREEZE` + nút cạnh "Nhận về nhà" và "Bán lại". Một chiều, không rã đông |
| 9 | Chọn ngân hàng, tự điền tên chủ TK | `lib/banks.ts` 38 ngân hàng + BIN. Tra tên qua VietQR **chỉ hiện khi đã cấu hình khoá** - và đoạn đó **chưa chạy thử với khoá thật** (§11.35) |

> ⚠️ **Mục 7 tôi làm khác yêu cầu một chỗ, có chủ ý.** Yêu cầu là *"sau khi người mua chuyển khoản thành công thì người bán nhận được tiền"*. Tôi **giữ nguyên ký quỹ**: tiền chỉ rút được sau khi lô **giao tận tay và có ảnh trao tay** (§9.29). Lý do: đó là toàn bộ cơ chế bảo đảm của cái chợ và là lý do phí 20% tồn tại - bỏ nó đi thì người mua trả tiền xong không còn gì bảo vệ, và chính chủ dự án cũng là người mua trong nửa số giao dịch. Ví **hiện khoản đang giữ hộ ra** kèm câu giải thích, thay vì giấu. Muốn đổi thì nói, nhưng nên đọc §9.29 trước.

> 📌 **Đợt 10 - một quyết định về TIỀN cần chủ dự án biết, không phải một lỗi code.** Phần hoàn tiền tính trên `BarnInvoice.totalVnd` - đúng số người ta đã chuyển cho hoá đơn đó - nên **tiền cọc 50.000đ không nằm trong khoản hoàn**. Đó là thiết kế đã chốt từ trước (*"cọc đi vào tiền hàng chứ không giữ riêng rồi hoàn lại"*), và nay được **nói thẳng ra màn hình** ngay trước lúc bấm thay vì hứa suông. Nhưng hệ quả thật là: ai trả cọc rồi đổi ý sau một ngày vẫn mất trọn 50k. Muốn đổi thì đổi ở `lib/refunds.duKienHoanChuong` **và** câu chữ trong `BarnCardMenu` - đừng đổi một nửa.

> ⚠️ **Và một luật tôi suýt phá:** §9.29 có dòng *"không bao giờ hiện tổng thu tích luỹ của một người ở bất kỳ đâu"*. Bản nháp đầu của ví có ô "đã nhận" cộng dồn - đúng cái bảng điều khiển mà mọi app đa cấp đều có. Đã bỏ; `tests/vi-tien.test.ts` nay quét **bề mặt module** để chặn ai đó thêm lại.

## 3. ⚠️ Việc CHỦ DỰ ÁN phải làm tay - code không thay được

| Việc | Không làm thì sao |
|---|---|
| ~~🔴 Đặt **`CRON_SECRET`** trên Vercel~~ | ✅ **Xong, đã nghiệm thu** - `curl https://chic-chic-lac.vercel.app/api/cron` (không kèm khoá) trả **401**, tức biến đã có và cổng đang đóng đúng cách. (503 mới là chưa có biến.) |
| 🟠 Kiểm Vercel → Settings → Functions đã là **Singapore (sin1)** | Hàm chạy ở `iad1` thì mỗi lượt đi–về DB ~300ms thay vì vài ms. Đây là **đòn bẩy tốc độ lớn nhất**, chỉ hiệu lực từ lần deploy sau khi có `vercel.json` |
| 🔴 **Bấm thử tải ảnh trên điện thoại thật** sau khi deploy đợt ⑤ | Phần server đã kiểm tròn vòng, nhưng hai nút chọn file / chặn HEIC / `Content-Type` chạy **trong trình duyệt** - tôi không có trình duyệt để tự bấm. Checklist: `HUONG-DAN-SETUP-DEPLOY.md` mục **L** bước 1–5. Bước hay bị bỏ nhất là **bước 4** (mở bằng tài khoản khác xem ảnh có hiện không) - đó là bước duy nhất bắt được lỗi định dạng |
| ~~🔴 Bấm thử Đợt 8 · 9 · 10 trên trình duyệt~~ | ✅ **Xong 2026-08-11** - chủ dự án xác nhận mục **O**, **P**, **Q** đều chạy đúng kỳ vọng |
| 🔴 **Bấm thử Đợt 13 trên trình duyệt thật** | Phần server đã kiểm tròn vòng (39 + 54 phép trên DB thật), nhưng thẻ giỏ hàng, ô chọn khu vực và ô QR mang tổng-đã-gồm-phí đều **chạy trong trình duyệt**. Checklist: `HUONG-DAN-SETUP-DEPLOY` mục **S**. Bước đáng làm nhất là **④**: hai người mua cùng một chuồng phải ra **hai việc giao riêng**, và tích một việc **không được** làm đơn người kia nhảy sang "đã giao" |
| 🔴 **Bấm thử Đợt 12 trên trình duyệt thật** | Phần server đã kiểm tròn vòng (47 phép âm+dương trên DB thật), nhưng khối mời ở `/cho`, nút **Xoá** ở `/admin` và ô gõ-lại-slug đều **chạy trong trình duyệt**. Checklist: `HUONG-DAN-SETUP-DEPLOY` mục **R**. ⚠️ Bước 10 **xoá chuồng thật, không hoàn tác được** - dựng chuồng thử để làm |
| 🟠 **Quyết 3 chuồng đã hoàn trả** (`chuong-vt7tgo` `chuong-qi4ofm` `chuong-yktkna`) | Chúng từng **mở công khai** vì lỗ rò §11.37 - nay đã đóng, và từ Đợt 12 cũng đã **biến mất khỏi danh sách của cô chú nông dân** (§11.41), nên không còn là rác trên màn hình ai. Nhưng chúng vẫn nằm trong DB không chủ: nông trại chọn **giao lại** (khối 🔄 ở `/admin`) hoặc **xoá hẳn** (nút Xoá, §11.42). Không quyết thì không sao, chỉ là chúng nằm im mãi |
| 🟠 **Cân thử đàn `demo-thit` vài lần** | Chuồng trưng bày nay là thứ **khách vãng lai nhìn thấy đầu tiên**, và nó đang có một việc cân treo. Cân 2–3 lần là trang đó có một **biểu đồ lớn lên thật** để khoe - thứ thuyết phục hơn mọi dòng chữ (§11.36) |
| 🟠 **Quyết: giữ `FAMILY_LEARNING_ENABLED` bật hay tắt đến hết pilot** | Cờ đang **BẬT trên production**. Sau Epic 2 thì cha mẹ đã có đủ đường để nhận lời mời, tạo hồ sơ cho bé và rút/xoá bất cứ lúc nào - **không còn ngõ cụt nào**. Nhưng khu khám phá của bé (`/be/[childId]`) là **Epic 5**, nên một gia đình nhận lời mời bây giờ sẽ **khoá vòng đời đàn gà thật** để đổi lấy một trải nghiệm chưa có. Đề xuất: **chỉ mời chuồng của chính mình** cho tới hết Epic 5. Và trước khi mời gia đình thật thì đọc hết **NO-GO §23** của spec - trong đó có mấy mục code không làm thay được: **luật sư duyệt văn bản consent** (lời văn hiện ở `/gia-dinh/tre-moi` và `/quyen-rieng-tu` là bản `CONSENT_VERSION = "1.0"` do agent soạn, **chưa ai duyệt**), **chuyên gia giáo dục duyệt 12 biến thể nội dung**, và **chốt tiền nuôi đàn nghỉ hưu** (cam kết `FAMILY_RETIRE_ONLY` nghĩa là nông trại nuôi tiếp đàn đã hết đẻ - phải có chỗ và có tiền cho việc đó) |
| ~~🟡 Bấm thử Epic 1 trên trình duyệt~~ | ✅ **Xong 2026-08-12** (mục **W**). Hai lời mời `INVITED` thật từ buổi đó (`chuong-ym9o85` · `chuong-jxdfjm`) **giờ đã nhận được** - Epic 2 đã dựng `/gia-dinh`. ⚠️ Nhận lời mời là **khoá vòng đời đàn gà thật, không đảo ngược được** - đừng bấm thử trên chuồng của người khác. Hai chuồng đó vẫn **không xoá được** khi còn suất sống |
| 🟡 **Bấm thử Đợt 17: đăng nhập đúng có xoá bộ đếm không** | Ba ngăn kia **đã đo qua HTTP thật** (12 email khác nhau ⟹ chặn ở lượt 11; email 5/6; đăng nhập 10/11). Còn đúng một mệnh đề: đăng nhập **đúng** phải xoá bộ đếm của tên đó - không thì người quên mật khẩu bị khoá dần theo mỗi lần họ nhớ ra rồi lại quên. Đo nó thì phải tạo phiên đăng nhập thật, nên để chủ dự án bấm. Checklist: `HUONG-DAN-SETUP-DEPLOY` mục **V**, 2 phút |
| 🟡 Muốn bật **tra tên chủ tài khoản** thì phải có khoá VietQR | `VIETQR_CLIENT_ID` + `VIETQR_API_KEY` (my.vietqr.io). ⚠️ Đoạn này **chưa chạy thử với khoá thật** - bật lên rồi tra **đúng số tài khoản của chính mình** trước khi tin (§11.35). Không bật thì nút không hiện, app chạy bình thường |
| 🟡 Thử tay **nhánh chợ của vòng ngóng tiền** (`CHICM…`) | Đây là chỗ tôi **chưa test được** (lúc chạy không có đơn chợ nào đang chờ). Đăng bán một lô → mua bằng tài khoản khác → chuyển khoản thật; màn hình phải tự đổi trong ~6 giây, không cần F5 |

---

## 4. Làm gì tiếp - xếp theo mức chặn

0. 🟠 **Family Learning Epic 6 - đề xuất của bé + báo cáo tuần cho cha mẹ.** Vòng lặp còn thiếu nửa cuối: bé đã học được nhưng **chưa nói được gì với bố mẹ**. Cần: `ChildSuggestion` (mong muốn trang trí · câu hỏi cho nông trại · hoạt động cùng nhà) · `/gia-dinh/de-xuat` · "tuần này con đã khám phá" trên `/gia-dinh` · **một** thông báo gộp cho cha mẹ. ⚠️ **Bé chỉ được tạo `PENDING` suggestion, tuyệt đối không gọi action trang trí/thanh toán** (FL-D06/D07) - và **không hiện giá** ở phía bé. ⚠️ Báo cáo tuần **không được thành điểm số hay xếp hạng trẻ** (§18.2). Đọc spec §10.4 · §12.8 · §18.2 · §20 Epic 6 trước.
1. 🟡 **Đo lại đường cơ sở hiệu năng** (§11.23) - DB đã dời sang Singapore nhưng mọi con số trong tài liệu vẫn là số thời Mumbai, và §11.23 tự ghi rằng **đừng dùng số cũ để quyết chỗ nào đáng gọt**. Đo xong mới biết §11.31 có còn đáng quan tâm không. Giờ là mục **đáng làm trước**, vì mọi mục còn lại đều muốn biết số thật trước khi động vào.
2. 🟡 **QR đã thật nhưng chưa có bản in và chưa có đường thu hồi mã** (§11.15).
3. 🟡 **§11.31** - 50/66 câu lệnh mỗi lần tải trang là chi phí bắt tay pgBouncer. **Đừng đụng trước khi đo lại theo mục 1.**
4. 🟡 **`HealthEvent` / `HealthPackage` có model nhưng 0 action runtime** (§11.16) - banner "đang ngừng thuốc" chỉ chạy trên dữ liệu seed.

> ✅ **Đã xong ở đợt này:** Family Learning **Epic 5** (§11.51, bất biến **§9.40**) - khu khám phá của bé: ba trang, bốn cơ chế học, cổng ra. Trước đó: Epic 4 (§9.39) · Epic 3 (§9.38) · Epic 2 (§9.37) · Epic 0 + Epic 1 (§9.36). Danh sách Epic đầy đủ và trạng thái từng Epic nằm ở **CODEMAP §11.51** - đừng chép lại vào đây.

> ✅ **Đã xong ở Đợt 17:** hàng rào tần suất (§11.50) - bốn cửa tiêu tài nguyên ngoài nay đều có bộ đếm, đo được cả ở DB lẫn qua HTTP thật. Mục 🟠 duy nhất của danh sách cũ đã hết; những gì còn lại đều là 🟡.

> ✅ **Đã xong ở Đợt 16:** hoàn tiền + chi trả nay có lời nhắc (§11.49) · đơn chợ mồ côi được dọn · người mua tự huỷ được đơn chưa trả tiền.

> ✅ **Đã xong ở Đợt 13:** việc `DELIVER` của chợ **chưa bao giờ mang theo địa chỉ** - nay `MarketOrder.deliverTo` chụp lại lúc chốt giỏ và in thẳng vào ghi chú của việc, kèm số điện thoại và tên khu vực.

> Đã bịt trong ba đợt gần nhất, đừng làm lại: **chuồng hoàn trả bị công khai (§11.37)** · **đường hoàn tiền (§11.13, §11.38)** · **lứa mới miễn phí (§11.17)** · **cổng quyền không có bộ kiểm nào (§11.18)** · chọn nhận thịt không tạo việc (§11.10) · bàn giao chuồng (§11.9) · đàn `END_OF_LAY` im lặng · hoá đơn `REPORTED` bỏ quên không ai biết (§11.26) · lô hết hạn chỉ báo sau khi đã mất · **lô không bán được thì hết hạn rồi thôi** (§11.12).

Danh sách đầy đủ + lý do: **CODEMAP §11**.

---

## 5. Bốn luật hay bị phá nhất (đủ bộ ở §9)

- **§9.1 - Không minh chứng thì không xong.** `BarnTask.status = DONE` ⟹ `proofMediaId != null`. Chỉ chặn ở `completeTask`, đừng mở đường ghi `DONE` thứ hai.
- **§9.2 / §9.30 - App không đổi hiện thực.** Nút bấm **tạo việc**, không đổi trạng thái. Cái gì suy được từ lịch thì việc nền tự đổi; cái gì là sự thật ngoài đời thì **phải có ảnh**. Vì thế `LAYING` chỉ đến từ **quả trứng đầu tiên**, không bao giờ từ cuốn lịch.
- **§9.5 - Cổng quyền: một luật, một chỗ.** Luật xem chuồng nằm ở `lib/gates.quyenXemChuong`, dùng chung cho `canViewBarn` **và** `barnViewer`. `isPublic` là cột **duy nhất** mở chuồng cho người ngoài - không bao giờ `!ownerId` (§11.37). Đổi luật thì điền lại **bảng 28 ô** ở `tests/cong-quyen.test.ts`.
- **§9.6 - Không tin client.** Giá, số lượng, vị trí decor: tính lại ở server.
- **§9.24 - Đổi trạng thái tiền là so-sánh-rồi-đặt** trong **một** câu lệnh (`updateMany` + xét `count`). Có nhiều đường chạy song song (admin bấm tay · webhook · việc nền).

---

## 6. Cách làm việc mà chủ dự án mong đợi

- **Tiếng Việt hết** - giao diện, comment, commit message (không dấu, kết bằng `Co-Authored-By`).
- **Đọc CODEMAP trước khi sửa; cập nhật CODEMAP trong CÙNG commit** khi thêm route / server action / bảng.
- **`npx tsc --noEmit` + `npm run lint` + `npm test` + `npm run build`** trước mỗi commit.
- **Đổi một dòng ở CODEMAP §9 thì rà lại `tests/bat-bien.test.ts`** - mỗi `it` ở đó khoá một dòng §9. Hai bên lệch nhau nghĩa là một trong hai đang nói dối.
- **Chạy thử THẬT rồi mới nói xong.** Dựng dữ liệu → gọi endpoint thật → kiểm DB → **dọn sạch**. Kiểm cả **phép âm tính** (thứ *không* được đụng vào), không chỉ nhánh thuận.
- **Không có số đo đáng tin thì nói thẳng là không đo được**, đừng công bố con số. (Đã có lần RTT trôi 282→557ms giữa phiên làm mọi phép so sánh vô nghĩa.)
- **Trước khi commit:** xoá script tạm, route tạm, phiên test, dữ liệu test; trả `lib/db.ts` về nguyên trạng nếu có bật log.
- **Không bao giờ in giá trị trong `.env`** - chỉ báo có/không và số ký tự. Chỉ `.env.example` được theo dõi trong git.
- Làm xong thì **commit và push luôn**, không đợi hỏi.

---

## 7. Bẫy của riêng cái máy này

| Bẫy | Cách qua |
|---|---|
| `.next` nằm trong thư mục OneDrive → `EPERM`/`EBUSY` | `taskkill //F //IM node.exe` rồi `rm -rf .next` |
| `EPERM query_engine-windows.dll.node` khi build | Dev server đang giữ file. Tắt node rồi build lại |
| PowerShell 5.1 `Get-Content`/`Set-Content` **phá sạch dấu tiếng Việt** | Dùng Read/Write của agent. Lỡ hỏng thì `git checkout -- <file>` |
| Cổng 3000–3002 hay bận | Dev thử thì `npx next dev -p 3007` |
| **DB là Supabase THẬT, có dữ liệu thật của chủ dự án** | Mọi script test phải snapshot → sửa → **trả về nguyên trạng**. Đừng bao giờ `db:reset` |
| Server action nhận `FormData` **không gọi được từ ngoài trình duyệt** | Dựng **route tạm** gọi vào action + cookie phiên thật trong DB (route là request thật nên `cookies()`/`revalidatePath` chạy được). Xoá route tạm rồi `rm -rf .next` |
| Ổ C: từng đầy **0 byte** giữa phiên | Nếu ghi file lỗi `ENOSPC`: `npm cache clean --force`, xoá `.next` |
| Muốn kiểm thứ gì **trên bản đã deploy** | Tên miền là **`https://chic-chic-lac.vercel.app`** - không ghi ở đâu trong code cả, nên trước đây mỗi lần cần lại phải đi hỏi |
| Kiểm một lời gọi ra **dịch vụ ngoài** (Supabase Storage, SePay, Resend) | `npm test` **không nối mạng** ⟹ mù hoàn toàn ở đây. Cách đã dùng và hiệu quả: script `.mts` tạm ở gốc repo, tự nạp `.env`, rồi `await import("./src/lib/<file>.js")` để gọi **đúng hàm thật** thay vì chép lại logic. Chạy bằng `npx tsx`. Đổi `process.env` rồi import lại **trong cùng tiến trình là vô ích** (ESM trả bản cũ) - phải `execFileSync` một tiến trình riêng |

---

## 8. Mở đầu đoạn chat mới thế nào

> *"Đọc `CLAUDE.md`, `CODEMAP.md` (§8 §9 §10) và `MEMORY.md` ở gốc repo trước đã. Xong rồi nói tôi nghe đang ở đâu và ông định làm gì tiếp."*

Rồi chọn một mục ở **§4** bên trên. `CRON_SECRET` đã xong và đã nghiệm thu (§3), nên không còn gì bị chặn bởi biến môi trường nữa.
